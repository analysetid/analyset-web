---
interface Props {
  title: string;
  buttonLabel?: string;
  href?: string;
}

const {
  title,
  buttonLabel = 'Hubungi Kami Sekarang',
  href = '/contact/',
} = Astro.props;
---

<section class="py-12 md:py-16">
  <div class="max-w-7xl mx-auto px-4 md:px-8">
    <div
      class="relative overflow-hidden rounded-[2.5rem] bg-sky-gradient px-6 md:px-12 py-10 md:py-12 text-center text-white shadow-lg shadow-sky-400/30"
    >
      <div class="absolute inset-0 bg-grid-light opacity-30" aria-hidden="true"></div>
      <div
        class="absolute -top-20 -left-20 h-64 w-64 rounded-full bg-sky-300/25 blur-3xl"
        aria-hidden="true"
      ></div>
      <div
        class="absolute -bottom-24 -right-16 h-72 w-72 rounded-full bg-primary-dark/40 blur-3xl"
        aria-hidden="true"
      ></div>
      <div class="relative">
        <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold leading-tight max-w-3xl mx-auto">
          {title}
        </h2>
        <a
          href={href}
          class="mt-6 inline-block bg-white text-primary rounded-xl px-8 py-3 font-semibold shadow-md hover:-translate-y-0.5 hover:shadow-lg transition-all duration-300"
        >
          {buttonLabel}
        </a>
      </div>
    </div>
  </div>
</section>
